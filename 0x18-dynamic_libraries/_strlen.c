#include "main.h"

/**
*_strlen - checks for the string length
*@s: string to be iterated through
*Return: returns value 1
*/

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (1);
}

